import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        marginTop: 50,
    },
    infoBold: {
        color: '#808080',
        fontWeight: 'bold',
        fontSize: 16,
        marginTop: 10
    },
    infoLight: {
        color: '#808080',
        fontWeight: 'normal',
        fontSize: 14,
    }
})